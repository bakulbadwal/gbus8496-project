# AI-use note (deliverable #6)

Albert requires *"a short AI-use note in the notebook: which AI tools you used, for what, and how
you verified their output."* We keep it here as we go and copy the summary into the final notebook.
One line per session or task. Verification means something concrete: a test that ran, a number
recomputed by hand or by a second method, a manual read of the output.

| Date | Who | Tool / model | What it did | How we verified |
|---|---|---|---|---|
| 2026-09-02 | Bakul | Claude Code (Claude Fable 5.1) | Decoded Albert's assignment PDF and Sep 1 announcement into `docs/Final_Project_SPEC.md`; scored five candidate directions in `docs/Project_DIRECTION-MEMO.md`; scaffolded this repo | Spec quotes checked against the PDF text page by page; CUAD facts (510 contracts, 41 clause types, 13,000+ annotations, CC BY 4.0) checked live on atticusprojectai.org and the Hugging Face dataset card; v1 of the spec had listed 8 of the 9 examples, corrected |
| 2026-09-05 | Bakul | Claude Code (Claude Fable 5.1) | Chose the DonorsChoose direction after no team input by Saturday; drafted the one-page proposal (`docs/proposal/`) as .docx via docx-js | Dataset schema and file sizes verified by fetching the Hugging Face mirror's headers and first rows; one-page fit verified by rendering with Carlito (metric-identical to Calibri) at Word's page geometry; Canvas page re-read for the .doc/.docx requirement |
| 2026-09-06 | Bakul | Claude Code (Claude Fable 5.1) | Evaluated Rodolfo's Amazon Reviews 2023 proposal against the rubric, hardened it (leakage, user + costs, constructed label, text-vs-tabular), drafted proposal v2 (.docx); wrote the data fetch recipe | Dataset gating, per-category file sizes, and field names verified by HTTP HEAD and first-record reads from the Hugging Face hub; `rating_number`/`average_rating` leakage identified from the meta record itself; one-page fit checked with Calibri-metric fonts at Word's page geometry |
| 2026-09-07 | Bakul | Claude Code (Claude Fable 5.1) | Reviewed Malorie's *Second Gift* proposal against the rubric and the Amazon alternative; filled the blanks and tightened two facts (ICPSR account wording, label window closing before Dec 2019, payoff-derived threshold) into `Proposal_v3_FINAL.docx`; memo recommendation updated | Record counts (11,377,479 donations; 2,149,817 projects) checked against the DOI's DataCite registration; ICPSR study page could not be read (bot check), so the account requirement is stated as standard practice, not verified; one-page fit checked with Calibri-metric fonts |
| 2026-09-09 | Bakul | Claude Code (Claude Opus 5) | Built the shared skeleton for the data + label workstream: `src/config.py`, `src/labels.py`, `src/baselines.py`, `evals/score.py`, `evals/make_fixture.py`, and the annotated notebook `01_labels_and_baseline.ipynb` | No ICPSR data yet, so correctness was established against a synthetic fixture with known structure: the donor-ID check returns PASS where linking was built in; the unclosed-window filter drops exactly the 2019 cohorts; and the scorer passes an internal consistency test — a random ranking at capacity c captures c of the value (0.50→50.06%, 0.20→20.2%, 0.10→8.7%). Notebook executed end to end, 7 code cells, 0 errors. **Every fixture-derived number is meaningless and is labelled as such in the notebook.** |
| 2026-09-11 | Bakul | Claude Code (Claude Fable 5.1) | Real ICPSR data landed. Fixed two things the fixture could not reveal (tab separator; refunds with negative amounts); ran the donor-ID check → PASS; built `cohorts.parquet` on 11.4M rows; ran measurement 1; found that pooled results are dominated by ~700 corporate matching programs and added a population scoping default (`config.STAKEHOLDER_POPULATION`) plus `evals/profile_cohorts.py` to make that evidence re-runnable | Column names and value encodings read from the DS1 codebook PDF, not assumed; separator verified by counting tabs vs commas in the header; row count 11,377,479 matches the codebook exactly; the scorer's internal check still holds on real data (random ranking at 10% captures 8.9% of value, at 50% captures 46%); the whale finding is reproducible with one command and is stated as a decision for the team, not a fact about donors |
| 2026-09-11 | Bakul | Claude Code (Claude Fable 5.1) | Unowned deliverables: presentation outline (10 slides, owner + class-vote question per slide, Albert's two required answers and the disclosure placed), Bakul's two slides drafted with figures, exec-summary skeleton, and `evals/run_all.py` so every reported number reproduces from one command | Both figures are generated from the data by `docs/slides/make_charts.py`, not typed in; the two-hue palette passed the six-check colour validator (CVD ΔE 24.7, normal-vision 33.6); charts rendered and inspected — one title formatted a count as `708.0`, fixed and regenerated |
| 2026-09-11 | Bakul | Claude Code (Claude Fable 5.1) | Built the deck template `docs/slides/Group11_deck.pptx` with pptxgenjs in the house style: slides 1–4 and 6 with real content (title + disclosure, the 71% chart, the four label decisions, the three-populations chart, the baseline table with real rows), slides 5 and 7–10 as styled frames with each owner's brief | File validated with the pptx schema/relationship checker (all pass); chart palette validated on the dark surface (CVD ΔE 26.8); every slide rendered through Keynote and inspected — native PowerPoint charts did not render there, so both charts became images generated from the data; three layout defects found and fixed (a stat label wrapping into a footnote, stat cards overflowing, a title breaking under its chart). PowerPoint's own PDF export reported success without writing a file, so the preview PDF is Keynote's render |
| 2026-09-12 | Bakul | Claude Code (Claude Fable 5.1) | Unowned pieces: `tests/test_labels.py` (13 tests pinning every label rule on a hand-built fixture), `src/reference_model.py` (logistic floor a real model must beat; holdout scored once), `src/decision.py` (break-even threshold from payoffs, capacity vs threshold policies, cost sensitivity, a Monday list), `evals/check_submission.py` (pre-flight for Albert's six deliverables + zip build). Steps 06–07 added to `run_all.py` | Tests pass 13/13; every fixture donor's expected label was written down before the code ran. Reference model: thank-you-packet flag excluded to avoid leakage; coefficients read for sign sanity (bigger first gift → more return, gift-card → less, later cohorts → less). Decision layer: contact-everyone net matches 814,971 × ($6.30 − $25) by hand; the p* sensitivity table was checked against p* = cost/50 at each cost. Full chain reproduced in 1.4 min, outputs committed |
| 2026-09-17 | Bakul | Claude Code (Claude Fable 5.1) | Reviewed Reid's and Rodolfo's pushes; wrote `src/features.py` (Reid's projects join as a script) and ran Rodolfo's `src/model.py` on the real data for the first time; merged his branch to main; added step 08 to `run_all.py`; saved the dev-val output to `evals/results/08_model_dev.txt` | Join output checked against Reid's notebook cell by cell: 926,619 matched projects, 8,432 missing, 99.74% — identical. Model run reproduces Rodolfo's own design (fit ≤2015, evaluate 2016, holdout untouched); the gift-size baseline on dev-val ($117) is consistent with its holdout value ($116); the paired bootstrap Δ of $2.05 ± 0.67 was read as small-but-real, not as a win. Two defects found in Reid's notebook (NameError in cell 25; significance and value tables not produced by committed code) and one in Rodolfo's (classifier hit its iteration cap) — all reported to the owners, none silently fixed |
| 2026-09-19 | Bakul | Claude Code (Claude Fable 5.1) | `evals/calibration.py` — Albert's measurement 2 as a reusable script (calibration bins, ECE, Brier vs base rate, error analysis by cohort year and by first-gift size band, slide-7 figure in light and dark), run on the reference model's existing holdout scores; step 09 in `run_all.py` | No refit: the scores file already existed, so the holdout was read, not re-touched. Bin counts sum to 814,971 (every holdout citizen donor scored); the 2017/2018 precision values were cross-checked against the pooled 22.3% in `06_reference_model.txt` (their donor-weighted mean is 22.3%); Brier of the base-rate predictor recomputed by hand as p(1−p) = 0.1260 × 0.8740 = 0.1101; both figures rendered and inspected for label collisions |
| 2026-09-22 | Bakul | Claude Code (Claude Opus 5.5) | Integration pass with team approval: raised the model's iteration cap so early stopping fires (checked on dev-val first), scored the holdout once, made `src/decision.py` use the model's own per-donor expected value instead of a flat $50, ran decision layer and calibration on the model's scores (steps 10–12 in `run_all.py`), fixed the missing import in notebook 02 and executed notebooks 02 and 03, filled deck slides 6, 8, 10 and the one-page exec summary with holdout numbers | Dev-val before/after the cap change ($119.82 → $119.51) shows the cap was not binding, so the holdout was not tuned on. Decision-layer change regression-tested: on the reference scores (no expected_value column) every policy row is identical to the Sep 12 output. $312K gap checked by hand as $7,750,740 − $7,438,320 and against the paired Δ ($3.83 × 81,509 contacts). The exec-summary claim that her rule's list is entirely $100+ donors was computed (100.0%), not assumed. Deck rendered through Keynote and inspected; slide-10 stage note removed from the slide face. Exec summary rendered to PDF and confirmed one page |
| 2026-09-24 | Thadeus | Codex (GPT-6) | Aligned existing calibration/error analysis with the headline dollar ranking; added score-coverage validation, weighted subgroup precision, policy comparison and robustness checks; added annotated notebook 04 and regression tests; completed slides 7 and 9 with speaker notes using the existing deck builder; corrected the small-donor claim and qualified dollar-score and outreach-profit interpretations across shared materials | Full raw-data evaluation reproduced all 13 stages in 8.0 minutes with the original headline unchanged; 23 tests passed; all 7 notebook code cells executed with no errors; recalculated 449 / 81,509 = 0.55% small donors in the dollar-ranked list versus 14.4% in the probability-ranked list; deck package/layout/import checks passed and rendered slides and one-page summary inspected. Human review of interpretation and team approval remain pending |

### Modeling workstream (Rodolfo)

- **Tool:** Claude (Anthropic), chat interface. Dates: Sep 16-17, 2026.
- **What it produced:** src/model.py (P(return) classifier + log-amount regressor, expected-value
  ranking, train-only dev loop with a 2015/2016 time split, bootstrap noise floor, gated --holdout
  flag) and notebooks/03_model_development.ipynb (annotated run of the above).
- **How it was checked before use:** the pipeline was executed end to end on the synthetic fixture
  (evals/make_fixture.py) twice — once on donations-only features, where rank-by-gift-size
  correctly remained the best ranking (the fixture's only planted signal is gift size), and once
  with fabricated project columns carrying a planted state x gift-size interaction, which the
  model recovered (dev-val $/contact ~4.9x the bootstrap SE above the gift-size baseline) while
  the baseline could not. No real-data number in the repo was produced by the AI session; all
  fixture outputs were discarded per the fixture's own warning.
- **Human responsibility:** Rodolfo runs it on the real cohorts_with_projects.parquet, reviews the
  dev-val table with the team, and scores the holdout once after sign-off.
